def find_mentor(i, lst):    
    lst[]

def main():
    n = int(input())
    for i in range(n):
        group = input().split()
        group = list(map(lambda i: int(i), group))
        mentees = list(map(lambda i: find_mentor(i, group)), group)
        
main()